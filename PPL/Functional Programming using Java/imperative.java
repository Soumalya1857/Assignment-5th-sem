// checkout java.util.function  for all the functional interfaces supported by java

import java.util.*;
import java.util.function.Function;


class test{

    public static void main(String args[])
    {

//         List<Person> female = people.stream()
//                             .filter(person-> isFemale())
//                             .collect(Collection.toList())
//                             .forEach(System.out.println);

//         int num = incrementByOne.apply(45);

        
//         // function chainning using andThen
//         // Function<int,int> nibba = function1.andThen(function2)
//         // nibba.apply(1,2);
//         predicate.test();
//         // for predicate we can chaining using (and / or)
//         Function.apply();
//         consumer.accept();

//         numbers.stream().filter(number-> isEven()).collect(toList()).filter(number-> isUnique()).collect(toList());



//         words.stream().filter(word-> Character.isDigit(word.charAt(word.length()-1))).collect(toList())
//     }

//     statiFunctionon<Integer, Integer> incrementByOne = number -> number++;
// }

// collection.frequency(list,)

    }
}